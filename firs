CREATE OR REPLACE FUNCTION api.get_cities_by_country_id(country_id INTEGER) RETURNS TABLE (identifier INTEGER, description TEXT) AS $$
BEGIN
    RETURN QUERY
    SELECT identifier, description
    FROM api.cities
    WHERE country_id = country_id;
END;
$$ LANGUAGE plpgsql;

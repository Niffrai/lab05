ERROR:  column "date" does not exist
LINE 1: SELECT MIN(date) AS earliest_date, MAX(date) AS latest_date
                   ^
QUERY:  SELECT MIN(date) AS earliest_date, MAX(date) AS latest_date
    FROM data.measurement_all
    WHERE city_id = city_id
CONTEXT:  PL/pgSQL function api.get_measurement_time_range(integer) line 3 at RETURN QUERY 

SQL state: 42703

CREATE OR REPLACE FUNCTION api.get_country_cities_count()
RETURNS SETOF RECORD AS $$
BEGIN
    RETURN QUERY
    SELECT ROW(country::INTEGER, COUNT(*))
    FROM data.city
    GROUP BY country;
END;
$$ LANGUAGE plpgsql;
